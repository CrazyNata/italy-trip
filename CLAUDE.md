# Правила проекта

- **Git-процесс:** всегда коммитить и пушить прямо в основную ветку `main`
  (владелец называет её «мастер»). Отдельные feature-ветки в этом проекте
  не нужны — работаем напрямую в `main`, чтобы результат сразу был виден.
