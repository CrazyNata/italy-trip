# Фильтр приоритетных ресторанов

Добавить в панель ресторанов отдельный фильтр `🔥 приоритет`. Он показывает только записи со статусом `🔥 приоритет` и сочетается с городом, районом, ценой и рейтингом. Новая вкладка не создаётся.
