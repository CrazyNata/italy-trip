# Типы мест в ресторанах

Добавить к карточкам тип `ресторан`, `кафе` или `бар`. В редакторе появится выбор типа, в списке — фильтр. Существующие записи получают тип `ресторан`; бары и кафе используют те же рейтинг, карту, фото и бронирование.
