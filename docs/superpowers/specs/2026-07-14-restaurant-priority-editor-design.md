# Огонёк в редакторе ресторана

Добавить в редактор ресторана отдельный переключатель `🔥 приоритет`. Он сохраняет основной статус (`хочу`, `бронь`, `были`) и записывает приоритет отдельным булевым полем, по которому работает фильтр приоритетных ресторанов.
